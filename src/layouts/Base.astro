---
import '../styles/global.css';
import '../styles/prose.css';
import '../styles/syntax.css';
import Navbar from '../components/nav/Navbar.astro';

interface Props {
  title: string;
  description?: string;
  hideNavbar?: boolean;
}

const { title, description = "Plateforme collaborative M2 GL Cloud", hideNavbar = false } = Astro.props;
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title} | GL·Cloud</title>
  </head>
  <body class="bg-[var(--surface)] text-[var(--text)] font-mono text-[13px] leading-relaxed flex flex-col min-h-screen">
    <div class="content relative z-10 flex-grow flex flex-col">
      {!hideNavbar && <Navbar />}
      <main class="flex-grow w-full">
        <slot />
      </main>
    </div>
    <footer class="px-6 md:px-12 lg:px-20 xl:px-32 py-8 mt-auto border-t border-[var(--ghost-border)] w-full max-w-[1200px] mx-auto flex flex-col md:flex-row md:items-center justify-between gap-6 md:gap-0 z-20 relative bg-[var(--surface-container-low)]">
      <div>
        <div class="font-serif font-medium text-[20px] text-[var(--text)]">GL·Cloud</div>
        <div class="text-[11px] text-[var(--muted)] mt-2 tracking-wide font-mono">
          M2 Informatique · Open Source · The Algorithmic Manuscript
        </div>
      </div>
      <div class="flex gap-4">
        <a class="text-[11px] font-mono text-[var(--muted)] no-underline hover:text-[var(--secondary)] transition-colors duration-0" href="#">GitHub</a>
        <a class="text-[11px] font-mono text-[var(--muted)] no-underline hover:text-[var(--secondary)] transition-colors duration-0" href="/contribuer">Contribuer</a>
        <a class="text-[11px] font-mono text-[var(--muted)] no-underline hover:text-[var(--secondary)] transition-colors duration-0" href="#">Discussions</a>
      </div>
    </footer>
  </body>
</html>
